@model IEnumerable<GuideMe.Models.Group>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

<h2 class="text-center text-primary">Available Groups</h2>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" style="background-color: rgba(255, 255, 255, 0.9); border: 2px solid #dc3545; border-radius: 5px; padding: 15px; margin: 20px auto; max-width: 600px; color: #dc3545; font-weight: 500; box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);">
        <strong>Error!</strong> @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="text-end mb-3">
    <a asp-controller="GroupFormation" asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus-circle"></i> Create Group
    </a>
</div>

<div class="group-container">
    @foreach (var group in Model)
    {
        <div class="group-box">
            <h4 class="group-title"><i class="fas fa-users"></i> @group.Name</h4>
            <p><i class="fas fa-map-marker-alt"></i> @group.Location</p>
            <p><i class="fas fa-calendar-alt"></i> @group.TravelStartDate - @group.TravelEndDate</p>
            <p><i class="fas fa-map-marked-alt"></i> @group.Description</p>

            <div class="group-actions">
                @if (ViewData["JoinedGroups"] != null && ((List<int>)ViewData["JoinedGroups"]).Contains(group.GroupId))
                {
                    <div class="action-buttons">
                        <a asp-action="Edit" asp-controller="GroupFormation" asp-route-groupId="@group.GroupId" class="btn btn-warning">
                            <i class="fas fa-edit"></i> Edit
                        </a>
                        <a asp-action="Leave" asp-controller="GroupFormation" asp-route-groupId="@group.GroupId" class="btn btn-danger">
                            <i class="fas fa-sign-out-alt"></i> Leave
                        </a>
                    </div>
                }
                else
                {
                    <form asp-action="Join" asp-controller="GroupFormation" method="post">
                        <input type="hidden" name="groupId" value="@group.GroupId" />
                        <button type="submit" class="btn btn-success">
                            <i class="fas fa-user-plus"></i> Join
                        </button>
                    </form>
                }
            </div>
        </div>
    }
</div>

<!-- Chat Section -->
<div class="chat-section">
    @foreach (var group in Model)
    {
        @if (ViewData["JoinedGroups"] != null && ((List<int>)ViewData["JoinedGroups"]).Contains(group.GroupId))
        {
            <h4 class="text-center text-primary">Group Chat</h4>
            <div class="chat-container">
                <div class="group-info">
                    <h5><i class="fas fa-users"></i> @group.Name</h5>
                    <ul class="members-list">
                        @foreach (var member in group.GroupMembers)
                        {
                            <li>
                                <img src="@(!string.IsNullOrEmpty(member.User.UserImage)
                                          ? Url.Content("~/UserFile/" + member.User.UserImage)
                                          : Url.Content("~/UserFile/default-profile.jpg"))"
                                     alt="@member.User.Name's Profile Image" class="profile-img" />
                                <span>@member.User.Name</span>
                            </li>
                        }
                    </ul>


                </div>

                <div class="chat-box-container">
                    <div id="chatMessages-@group.GroupId" class="chat-box"></div>
                    <div class="chat-input">
                        <input type="text" id="messageInput-@group.GroupId" placeholder="Type a message..." />
                        <label for="fileInput-@group.GroupId" class="file-label">
                            <i class="fas fa-paperclip"></i>
                            <span class="file-count"></span>
                        </label>
                        <input type="file"
                               id="fileInput-@group.GroupId"
                               multiple
                               class="file-input"
                               accept="image/*,video/*,.pdf,.doc,.docx,.xls,.xlsx,.txt" />
                        <button onclick="sendMessage(@group.GroupId)" class="send-btn">
                            <i class="fas fa-paper-plane"></i>
                        </button>
                    </div>
                </div>
               
            </div>
        }
    }
</div>

<style>

    body {
        font-family: 'Poppins', sans-serif;
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        background-attachment: fixed;
        margin: 0;
        padding: 0;
        min-height: 100vh;
    }

    h2.text-center.text-primary {
        display: flex;
        justify-content: center;
        align-items: center;
        font-size: 3rem;
        margin-bottom: 30px;
        color: black;
        text-transform: uppercase;
        letter-spacing: 2px;
        border: none;
    }

    .text-end.mb-3 {
        position: absolute;
        right: 30px;
        top: 100px;
        height: auto;
    }

    .btn-primary {
        text-decoration: none;
        font-size: 1.2rem;
        padding: 12px 25px;
    }

    .group-box form {
        display: flex;
        justify-content: center;
        margin-top: 20px;
    }

    .group-box .btn-success {
        padding: 15px 40px;
        font-size: 1.2rem;
        width: auto;
        margin: 0 auto;
    }

    .text-end.mb-3 .btn-primary {
        text-decoration: none;
        font-size: 1.4rem;
        padding: 15px 30px;
        background: rgba(255, 255, 255, 0.8);
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        border: none;
        transition: all 0.3s ease;
    }

        .text-end.mb-3 .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

    .action-buttons a {
        text-decoration: none !important;
        font-size: 1.2rem;
        padding: 12px 25px;
        transition: all 0.3s ease;
    }

    .chat-section h4.text-center.text-primary {
        color: white !important;
        font-size: 3.5rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-bottom: 35px;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
    }

    .group-container {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 20px;
        margin-top: 20px;
    }

    .group-container {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 30px;
        margin: 40px auto;
        padding: 20px;
        max-width: 1400px;
    }

    .group-box {
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(10px);
        padding: 35px;
        border-radius: 15px;
        box-shadow: 0 8px 32px rgba(31, 38, 135, 0.15);
        border: 1px solid rgba(255, 255, 255, 0.18);
        text-align: center;
        transition: all 0.4s ease;
    }

        .group-box:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.9);
            box-shadow: 0 15px 45px rgba(31, 38, 135, 0.2);
        }

    .group-title {
        color: #2c3e50;
        font-size: 2.2rem;
        font-family: 'Poppins', sans-serif;
        margin-bottom: 25px;
        border-bottom: 2px solid rgba(75, 143, 140, 0.5);
        padding-bottom: 15px;
        text-align: center;
    }

    .group-box p {
        font-size: 1.3rem;
        font-family: 'Roboto', sans-serif;
        color: #34495e;
        margin: 15px 0;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
    }

    .group-box i {
        font-size: 1.6rem;
        margin-right: 15px;
        color: #4b8f8c;
        width: 30px;
        text-align: center;
    }

    .action-buttons {
        display: flex;
        gap: 20px;
        justify-content: center;
        margin-top: 30px;
    }

        .action-buttons a,
        .action-buttons form button,
        .btn-success {
            padding: 12px 30px;
            font-size: 1.1rem;
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            border-radius: 10px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            gap: 10px;
            backdrop-filter: blur(5px);
        }

    .btn-warning {
        background: rgba(241, 194, 50, 0.9);
        color: #333;
    }

    .btn-danger {
        background: rgba(201, 62, 62, 0.9);
        color: white;
    }

    .btn-success {
        background: rgba(111, 164, 77, 0.9);
        color: white;
    }

        .btn-warning:hover,
        .btn-danger:hover,
        .btn-success:hover {
            transform: translateY(-3px);
            filter: brightness(110%);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }


    .chat-section {
        margin-top: 30px;
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    /* Chat Container */
    .chat-container {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        width: 90%;
        max-width: 1200px;
        background-color: #ffffff;
        padding: 25px;
        border-radius: 8px;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        margin-bottom: 20px;
    }

    .chat-section h4 {
        color: #4b8f8c;
    font-size: 2rem;
    margin-bottom: 20px;
    }

    .group-info {
        width: 30%;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        padding-right: 20px;
        border-right: 2px solid #ddd;
        margin-top: 0;
    }

        .group-info h5 {
            color: #4b8f8c;
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

    .members-list li {
        display: flex;
        justify-content: flex-start;
        align-items: center;
        margin-bottom: 8px;
    }

    .members-list .profile-img {
        width: 45px;
        height: 45px;
        border-radius: 50%;
        margin-right: 10px;
    }

    .members-list span {
        font-size: 1.1rem;
        color: #333;
    }

    .chat-box-container {
        width: 65%;
        padding-left: 20px;
    }

    /* Chat Box */
    .chat-box {
        height: 500px;
        overflow-y: auto;
        border: 1px solid #ddd;
        padding: 15px;
        background: #f1f1f1; 
        margin-bottom: 15px;
        border-radius: 8px;
        box-shadow: inset 0px 2px 5px rgba(0, 0, 0, 0.05);
    }

    .chat-input input[type="text"] {
        flex-grow: 1;
        padding: 12px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 1.1rem;
        margin-right: 10px;
        width: 85%;
    }

    .chat-input .file-label {
        cursor: pointer;
        margin-left: 10px;
        font-size: 1.3rem;
    }

    .chat-input .file-input {
        display: none;
    }

    .chat-input .send-btn {
        padding: 12px;
        background-color: #4b8f8c;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1.3rem;
    }

        .chat-input .send-btn:hover {
            background-color: #3a6e68; 
        }

    .message-attachments {
        margin-top: 10px;
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
    }

    .image-attachment img {
        max-width: 200px;
        max-height: 200px;
        border-radius: 8px;
        cursor: pointer;
        transition: transform 0.2s;
        position: relative;
        display: inline-block;
        margin: 5px;
    }

        .image-attachment img {
            max-width: 200px;
            max-height: 200px;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.2s;
        }

    .image-actions {
        position: absolute;
        top: 5px;
        right: 5px;
        display: none;
        background: rgba(0, 0, 0, 0.7);
        border-radius: 4px;
        padding: 5px;
        z-index: 100;
        flex-direction: row;
        gap: 10px;
    }

    .image-attachment:hover .image-actions {
        display: flex;
    }

    .action-btn {
        background: none;
        border: none;
        color: white;
        cursor: pointer;
        padding: 5px;
        font-size: 16px;
        transition: color 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .action-btn:hover {
            color: #ffd700;
            transform: scale(1.1);
        }

    .message-attachments {
        margin-top: 10px;
        margin-left: 35px;
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
    }

    .video-attachment video {
        max-width: 300px;
        border-radius: 8px;
    }

    .file-attachment {
        padding: 8px 12px;
        background: #f0f0f0;
        border-radius: 4px;
        display: inline-flex;
        align-items: center;
        gap: 8px;
    }

    .image-modal {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.8);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 1000;
        cursor: pointer;
    }

        .image-modal img {
            max-width: 90%;
            max-height: 90%;
            object-fit: contain;
        }


    .message-header {
        margin-bottom: 5px;
    }

        .message-header .user-avatar {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            object-fit: cover;
            cursor: pointer;
        }

    .message-item {
        display: flex;
        flex-direction: column;
        margin-bottom: 10px;
    }

    .message-content {
        margin-left: 35px;
    }
</style>

<script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/6.0.3/signalr.min.js"></script>
<script>
    const connection = new signalR.HubConnectionBuilder()
        .withUrl("/chathub")
        .withAutomaticReconnect()
        .build();

    connection.start().then(() => {
        console.log("SignalR Connected");
        joinGroups();
        loadExistingMessages();
    }).catch(err => console.error("SignalR Connection Error:", err));

    function joinGroups() {
        document.querySelectorAll(".chat-container").forEach(container => {
            let groupId = container.querySelector(".send-btn").getAttribute("onclick").match(/\d+/)[0];
            connection.invoke("JoinGroup", parseInt(groupId))
                .catch(err => console.error("JoinGroup error:", err));
        });
    }

    function loadExistingMessages() {
        document.querySelectorAll(".chat-container").forEach(container => {
            let groupId = container.querySelector(".send-btn").getAttribute("onclick").match(/\d+/)[0];
            fetch(`/GroupFormation/GetMessages?groupId=${groupId}`)
                .then(response => response.json())
                .then(messages => {
                    let chatBox = document.getElementById("chatMessages-" + groupId);
                    chatBox.innerHTML = "";
                    messages.sort((a, b) => new Date(a.sentAt) - new Date(b.sentAt));
                    messages.forEach(msg => displayMessage(msg, chatBox));
                })
                .catch(err => console.error("Error loading messages:", err));
        });
    }

    function displayMessage(msg, chatBox) {
        const messageElement = document.createElement("div");
        messageElement.className = "message-item";
        messageElement.setAttribute('data-message-id', msg.messageId || Date.now());

        // Ensure proper handling of image URL
        let userImage;
        if (msg.userImageUrl && msg.userImageUrl.trim() !== '') {
            userImage = msg.userImageUrl.includes('UserFile')
                ? msg.userImageUrl
                : `/UserFile/${msg.userImageUrl}`;
        } else {
            userImage = '/UserFile/default-profile.jpg';
        }

        let messageHtml = `
            <div class="message-header">
                <img src="${userImage}" alt="${msg.userName}" class="user-avatar" title="${msg.userName} - ${new Date(msg.sentAt).toLocaleString()}" />
            </div>`;

        if (msg.messageText && msg.messageText.trim() !== '') {
            messageHtml += `<div class="message-content">${msg.messageText}</div>`;
        }

        if (msg.attachment) {
            const attachments = msg.attachment.split(',').filter(a => a.trim());
            if (attachments.length > 0) {
                let attachmentsHtml = '<div class="message-attachments">';
                attachments.forEach(attachment => {
                    const fileExt = attachment.toLowerCase().split('.').pop();
                    if (['jpg', 'jpeg', 'png', 'gif'].includes(fileExt)) {
                        attachmentsHtml += `
                            <div class="image-attachment">
                                <div class="image-actions">
                                    <a href="${attachment}" download title="Download" class="action-btn">
                                        <i class="fas fa-download"></i>
                                    </a>
                                    <button onclick="deleteAttachment(this, '${attachment}')" title="Delete" class="action-btn">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </div>
                                <img src="${attachment}" alt="Image" onclick="openImageModal(this.src)" />
                            </div>`;
                    } else if (['mp4', 'webm', 'ogg'].includes(fileExt)) {
                        attachmentsHtml += `
                            <div class="video-attachment">
                                <video controls>
                                    <source src="${attachment}" type="video/${fileExt}">
                                    Your browser does not support the video tag.
                                </video>
                            </div>`;
                    } else {
                        attachmentsHtml += `
                            <div class="file-attachment">
                                <a href="${attachment}" target="_blank" download>
                                    <i class="fas fa-file"></i> ${attachment.split('/').pop()}
                                </a>
                            </div>`;
                    }
                });
                attachmentsHtml += '</div>';
                messageHtml += attachmentsHtml;
            }
        }

        messageElement.innerHTML = messageHtml;
        chatBox.appendChild(messageElement);
        chatBox.scrollTop = chatBox.scrollHeight;
    }

    async function deleteAttachment(button, attachmentPath) {
        if (!confirm('Are you sure you want to delete this image?')) return;

        try {
            const response = await fetch('/GroupFormation/DeleteAttachment', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ path: attachmentPath })
            });

            if (response.ok) {
                button.closest('.image-attachment').remove();
            } else {
                alert('Failed to delete the image');
            }
        } catch (error) {
            console.error('Error:', error);
            alert('Failed to delete the image');
        }
    }

    document.querySelectorAll('.file-input').forEach(input => {
        input.addEventListener('change', function() {
            const fileCount = this.files.length;
            const countSpan = this.previousElementSibling.querySelector('.file-count');
            countSpan.style.display = fileCount > 0 ? 'block' : 'none';
            countSpan.textContent = fileCount || '';
        });
    });

    async function sendMessage(groupId) {
        const messageInput = document.getElementById(`messageInput-${groupId}`);
        const fileInput = document.getElementById(`fileInput-${groupId}`);
        const message = messageInput.value.trim();
        const files = fileInput.files;

        if (!message && files.length === 0) return;

        const formData = new FormData();
        formData.append('messageText', message || '');
        formData.append('groupId', groupId);

        Array.from(files).forEach(file => {
            formData.append('files', file);
        });

        try {
            const response = await fetch('/GroupFormation/SendMessage', {
                method: 'POST',
                body: formData
            });

            if (!response.ok) throw new Error('Failed to send message');

            messageInput.value = '';
            fileInput.value = '';
            const countSpan = fileInput.previousElementSibling.querySelector('.file-count');
            if (countSpan) {
                countSpan.style.display = 'none';
                countSpan.textContent = '';
            }
        } catch (error) {
            console.error('Error:', error);
            alert('Failed to send message. Please try again.');
        }
    }

    document.querySelectorAll('[id^="messageInput-"]').forEach(input => {
        input.addEventListener('keypress', function(e) {
            if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                const groupId = this.id.split('-')[1];
                sendMessage(groupId);
            }
        });
    });

    connection.on("ReceiveMessage", function (groupId, userName, message, attachments, timestamp, imageUrl) {
        const chatBox = document.getElementById(`chatMessages-${groupId}`);
        if (!chatBox) return;

        let userImage;
        if (imageUrl && imageUrl.trim() !== '') {
            userImage = imageUrl.includes('UserFile')
                ? imageUrl
                : `/UserFile/${imageUrl}`;
        } else {
            userImage = '/UserFile/default-profile.jpg';
        }

        displayMessage({
            userName: userName,
            messageText: message,
            attachment: attachments,
            sentAt: timestamp,
            userImageUrl: userImage
        }, chatBox);
    });

    function openImageModal(src) {
        const modal = document.createElement('div');
        modal.className = 'image-modal';
        modal.onclick = () => modal.remove();
        modal.innerHTML = `<img src="${src}" alt="Full size image">`;
        document.body.appendChild(modal);
    }
</script>